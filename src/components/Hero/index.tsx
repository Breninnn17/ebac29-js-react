import { FormStyle, HeroTitle } from './style'

const Hero = () => (
  <FormStyle>
    <div className="container">
      <HeroTitle>As melhores vagas para tecnologia, estão aqui!!</HeroTitle>
    </div>
  </FormStyle>
)

export default Hero
